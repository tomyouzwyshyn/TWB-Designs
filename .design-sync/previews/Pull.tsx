import * as React from 'react';
import { Pull } from '@twb/design-system';

export const Default = () => (
  <Pull>TWB removes the transfers. Not one of them, all of them. The material is destroyed inside the perimeter of the organisation that owns it and never enters anyone else&rsquo;s hands.</Pull>
);
