import * as React from 'react';
import { Lede } from '@twb/design-system';

export const Default = () => (
  <Lede>Custody is not a feature of the machine. It is the reason to buy the machine.</Lede>
);
