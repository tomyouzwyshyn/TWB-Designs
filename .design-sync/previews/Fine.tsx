import * as React from 'react';
import { Fine } from '@twb/design-system';

export const Default = () => (
  <Fine>Contract references, scope and dates available under mutual NDA. Buyer diligence may verify directly with end users, subject to end-user consent and TWB coordination.</Fine>
);
