import * as React from 'react';
import { Checkbox } from '@twb/design-system';

export const Unchecked = () => <Checkbox>Send the one-page capability statement with the reply.</Checkbox>;
export const Checked = () => <Checkbox defaultChecked>Send the one-page capability statement with the reply.</Checkbox>;
